apiVersion: v1
kind: Pod
metadata:
  name: compliant-pod-drop-all
spec:
  containers:
    - name: nginx
      image: nginx:latest
      securityContext:
        capabilities:
          drop:
            - ALL
