apiVersion: v1
kind: Pod
metadata:
  name: noncompliant-pod-no-drop
spec:
  containers:
    - name: nginx
      image: nginx:latest
      securityContext:
        capabilities:
          add:
            - NET_ADMIN
