# CIS Control: 5.5.1
# Description: Manage users via IAM Authenticator or AWS CLI
# NOTE: This control is not enforceable via Kyverno policy. Stub only.

apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: cis-5.5.1-manage-users-via-iam-authenticator-or-aws-cli
  annotations:
    cis.kubernetes.io/id: "5.5.1"
    cis.kubernetes.io/description: "Manage users via IAM Authenticator or AWS CLI"
spec:
  rules: []