# CIS Control: 3.1.2
# Description: kubelet kubeconfig file owned by root:root
# Not enforceable by Kyverno; requires checking file ownership on nodes, which must be audited manually or via infrastructure as code (e.g., Terraform, Ansible).

apiVersion: kyverno.io/v1
kind: Policy
metadata:
  name: cis-3.1.2-kubelet-kubeconfig-file-owned-by-root-root
  annotations:
    cis.kubernetes.io/id: "3.1.2"
    cis.kubernetes.io/description: "kubelet kubeconfig file owned by root:root"
spec:
  background: false
  rules: []