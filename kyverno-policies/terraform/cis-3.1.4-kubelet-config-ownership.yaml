apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: terraform-cis-3.1.4-kubelet-config-ownership
  annotations:
    policies.kyverno.io/title: "Terraform CIS 3.1.4 - Kubelet config file ownership"
    policies.kyverno.io/category: Terraform IaC Scanning
    policies.kyverno.io/severity: high
    policies.kyverno.io/description: >-
      Validate that the kubelet config file is owned by root:root in Terraform plan.
spec:
  validationFailureAction: audit
  rules:
    - name: kubelet-config-ownership
      match:
        any:
          - resources:
              kinds:
                - TerraformPlan
      validate:
        message: "Kubelet config file must be owned by root:root."
        pattern:
          plan:
            resource_changes:
              - type: aws_eks_cluster
                change:
                  after:
                    resources:
                      - type: file
                        owner: "root"
                        group: "root"