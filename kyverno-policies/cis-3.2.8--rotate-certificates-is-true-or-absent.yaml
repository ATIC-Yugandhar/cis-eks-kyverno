# CIS Control: 3.2.8
# Description: --rotate-certificates is true or absent
# NOTE: This control is not enforceable via Kyverno policy. Stub only.

apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: cis-3.2.8-rotate-certificates-is-true-or-absent
  annotations:
    cis.kubernetes.io/id: "3.2.8"
    cis.kubernetes.io/description: "--rotate-certificates is true or absent"
spec:
  rules: []