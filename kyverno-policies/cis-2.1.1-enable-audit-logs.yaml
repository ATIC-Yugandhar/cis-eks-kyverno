# CIS Control: 2.1.1
# Description: Enable audit logs
# Not enforceable by Kyverno; requires enabling audit logs at the Kubernetes API server level, which must be configured via cluster setup or infrastructure as code (e.g., Terraform) or manual audit.

apiVersion: kyverno.io/v1
kind: Policy
metadata:
  name: cis-2.1.1-enable-audit-logs
  annotations:
    cis.kubernetes.io/id: "2.1.1"
    cis.kubernetes.io/description: "Enable audit logs"
spec:
  background: false
  rules: []