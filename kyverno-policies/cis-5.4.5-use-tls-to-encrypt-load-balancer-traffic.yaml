# CIS Control: 5.4.5
# Description: Use TLS to encrypt load balancer traffic
# NOTE: This control is not enforceable via Kyverno policy. Stub only.

apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: cis-5.4.5-use-tls-to-encrypt-load-balancer-traffic
  annotations:
    cis.kubernetes.io/id: "5.4.5"
    cis.kubernetes.io/description: "Use TLS to encrypt load balancer traffic"
spec:
  rules: []