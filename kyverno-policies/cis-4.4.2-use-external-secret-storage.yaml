# CIS Control: 4.4.2
# Description: Use external secret storage
# NOTE: This control is not enforceable via Kyverno policy. Stub only.

apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: cis-4.4.2-use-external-secret-storage
  annotations:
    cis.kubernetes.io/id: "4.4.2"
    cis.kubernetes.io/description: "Use external secret storage"
spec:
  rules: []